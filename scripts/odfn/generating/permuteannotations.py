from utils_odfn import seeds_plus, seeds_plus_spilt, seeds_plus_shuffled_dict
import random
import numpy as np
print(seeds_plus_shuffled_dict)
# tmp = []

# sub_seed = seeds_plus_spilt('train')
# random.shuffle(sub_seed)
# tmp.extend(sub_seed)
# sub_seed = seeds_plus_spilt('val')
# random.shuffle(sub_seed)
# tmp.extend(sub_seed)
# sub_seed = seeds_plus_spilt('test')
# random.shuffle(sub_seed)
# tmp.extend(sub_seed)



